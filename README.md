album_management.py

This file creates a list of class objects (specifically a list of of albums) and sorts them based on different criteria. The code makes use of class definition (with initialized 
variables a __str__ method), the sort() function, list indexing, and binary search. 
